# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(artoolkitTaskLib)
ADD_LIBRARY(${ARTOOLKIT_TASKLIB_NAME} SHARED 
    ${ARTOOLKIT_TASKLIB_SOURCES})
add_dependencies(${ARTOOLKIT_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ARTOOLKIT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ARTOOLKIT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ARTOOLKIT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ARTOOLKIT_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${ARTOOLKIT_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${ARTOOLKIT_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ARTOOLKIT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ARTOOLKIT_TASKLIB_HEADERS}
    DESTINATION include/orocos/artoolkit)

